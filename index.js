/* 

JS PLAN:

JS to place marker when selects a square, but can't over ride or switch already selected squares
Check for Win/Draw
Switch Turns

*/

const boxEvent = document.querySelectorAll('div.grid.box')
